//-вложить в лейбл
//-если вложить в лейбл, checked не наступает
mixin radio-button({name, id, text})
    .radio__inner
        input.radio__input(type="radio" name=name id=id)
        label.radio__label(for=id)=text