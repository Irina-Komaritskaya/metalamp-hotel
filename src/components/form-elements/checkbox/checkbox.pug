//- не нравится две обертки
mixin checkbox({name, text, title})
    label.checkbox__label
        input.checkbox__input(type="checkbox" name=name)
        .checkbox__inner
            .checkbox-text__inner
                if title
                    .checkbox__title=title
                .checkbox__text=text